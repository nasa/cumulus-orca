---
version: 2
plan:
  project-key: ORCA
  key: ORCA
  name: test-orca
stages:
- Test stage:
    manual: false
    final: false
    jobs:
    - Test job
Test job:
  key: TJ
  tasks:
  - checkout:
      force-clean-build: 'false'
      description: Checkout Default Repository
  - script:
      interpreter: SHELL
      scripts:
      - |-
        echo "Hello from bamboo"
      description: Test script
repositories:
- ORCA repo:
    type: git
    url: https://github.com/nasa/cumulus-orca
    branch: feature/ORCA-260
    # command-timeout-minutes: '180'
    # lfs: 'false'
    # verbose-logs: 'false'
    # use-shallow-clones: 'false'
    # cache-on-agents: 'true'
    # submodules: 'false'
    # fetch-all: 'false'
triggers:
- polling:
    period: '60'
branches:
  create: for-new-branch
  delete:
    after-deleted-days: 7
    after-inactive-days: 30
---
version: 2
plan:
  key: ORCA-RHASSAN
plan-permissions:
- users:
  - rizbi.hassan
  - andrew.dorn
  permissions:
  - view
  - edit
  - build
  - clone
  - admin
...
